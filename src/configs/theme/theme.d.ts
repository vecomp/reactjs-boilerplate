export interface ThemeProps {
    colors: {
        background: string;
        primary: string;
        accent: string;
        success: string;
        warning: string;
        danger: string;
        solid: {
            white: string;
        };
        text: {
            primary: string;
            secondary: string;
        };
    }
};