import {ThemeProps} from "./theme";

const theme: ThemeProps = {
    colors: {
        background: '#fefefe',
        primary: '#004ba0',
        text: {
            primary: '#212121',
            secondary: '#dddddd'
        }
    }
}

export default theme;